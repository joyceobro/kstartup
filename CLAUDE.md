# CLAUDE.md — "근거": 초기 벤처기업 심사 스크리닝 엔진 (기관 심사역용)

> 이 파일은 프로젝트의 설계 원칙과 확정 사항을 담는다.
> 새 세션은 반드시 이 파일을 먼저 읽고 작업한다. 원칙에 어긋나는 구현은 하지 않는다.

## 0. 제품 정의 & 대회 맥락

### 0.1 제품 한 줄 정의 (2026-08-27 재정의)

**"근거"** — 공개데이터 기반 · 출처 역추적형 **초기 벤처기업 심사 스크리닝 엔진**.
재무제표가 없는 초기 기업을 평가해야 하는 **기관 심사역**을 위한 도구다.

- **핵심 사용자**: 정책금융·기술보증기금/신용보증기금·은행 여신 심사역, 액셀러레이터·창업지원기관 서류 심사역. (VC 아님)
- **사용 목적**: ① 접수 기업 1차 스크리닝 ② 심층심사 질의서 시드 ③ 심사 판단의 대외 소명(심사위·내부감사·정책자금 국정감사) 근거 확보.
- **산출물 3종**:
  1. 출처 링크가 붙은 3축 팩트시트 (심사의견서에 그대로 첨부 가능한 형태)
  2. AI가 판정하지 않는 결정론적 집계 스코어 (집계식·규칙 공개)
  3. 자기서술 vs 공개기록의 모순을 잡는 6규칙 "확인 필요" 플래그 → 각 플래그를 심층심사 질의 문항으로 변환 출력
- **프레임 전환**: "이 회사 얼마나 좋아?"가 아니라 **"자기서술이 공개 기록과 맞나? 심사역은 어디를 더 파야 하나?"**
- **VC 매칭**은 MVP 주력이 아니라 **부가 데모**로 축소한다 ("관심 가질 만한 투자자·후속 지원사업 힌트" 수준).

### 0.2 대회 맥락

- **대회**: 2026 금융 AI Challenge (금융보안원 주최, 데이콘 운영)
- **주제**: AI 기반 금융 현안 해결 웹서비스 — "AI와 비정형 데이터 분석을 통한 혁신·벤처기업의 다차원 성장성 평가 및 투자 매칭"
- **참가**: 개인(솔로), 풀스택
- **마감**: 기획서 + MVP(기능명세서 + 배포 URL) — 2026-09-07 오전 10:00
- **배포 URL 생존 필수 구간**: 2026-09-07 11:00 ~ 09-11 23:59 (접근 불가 시 결격)
- **발표 심사**: 2026-10-13 (상위 11팀 내외)

## 1. 핵심 설계 원칙 (절대 훼손 금지)

**Non-self-grading — AI가 점수를 판정하지 않는다.**
- 점수는 LLM의 판단이 아니라 **공개 데이터 원천값의 결정론적 집계**다.
- 모든 점수 옆에는 **출처(원천 데이터 + 링크)**가 붙어 역추적 가능해야 한다.
- LLM이 "이 회사는 8점"이라고 매기는 구조를 만들지 않는다. LLM은 근거를 **설명**만 한다.

**LLM은 딱 필요한 곳에만.**
- 이 파이프라인은 본질적으로 결정론적이다. LangGraph 등 에이전트 아키텍처는 **쓰지 않는다** (오버엔지니어링, 3주 리스크).
- LLM의 유일한 역할: 최종 리포트의 자연어 근거 서술. (뉴스 감성분석은 소스에서 제외되어 현재 불필요.)

**데이터 부재를 처벌하지 않는다.**
- 초기 기업은 재무·특허가 없는 게 정상이다. "없음 = 저평가"로 처리하지 않는다.
- 반증은 "없음"이 아니라 **"모순"**을 잡는다. (예: 딥테크 표방인데 특허 0 = 모순)

**심사를 대체하지 않는다 — 심사역을 보조한다.**
- 최종 등급·여신 판단은 심사역과 심사위원회의 몫이다. 서비스는 근거 정리와 모순 지목까지만 한다.
- 모든 스코어·플래그는 원천값 + 링크를 동반해 "심사의견서에 그대로 첨부 가능한 형태"로 출력한다.

## 2. 평가 축 (3축 + 보조)

| 축 | 데이터 근거 |
|---|---|
| 기술력 | 특허 출원/등록 건수, IPC 분류, 벤처확인 "연구개발유형" |
| 투자이력 | 벤처확인 공시의 투자유치 금액·시기 |
| 공신력 | 정부인증 다층 보유(벤처/이노비즈/메인비즈), 인증유형·유효기간 |
| (보조) 재무 | OpenDART — 있으면 표시, 없으면 D1 중립 표기 |

- 뉴스 기반 "시장 관심도" 축은 **제외됨** (빅카인즈 유료 전환, 네이버 검색 API 종량제 전환 → 무료·안정 소스만 쓴다는 원칙에 따라 드롭).
- "활동성(momentum)" 4번째 축 아이디어는 기획서의 **향후 확장 방향**으로만 기재.

## 3. 반증 엔진 규칙셋 (v1, 6규칙)

red flag는 **감점이 아니라 "확인 필요" 플래그**다. 최종 판단은 사람(심사역·심사위원회)에게 남긴다. 각 플래그는 "심층심사에서 물어볼 질문" 형태로도 출력한다.

**A. 기술 과장**
- A1. R&D/딥테크 표방 + 특허 0건 (또는 출원 후 장기 정체)
- A2. 특허 출원인이 법인이 아니라 대표 개인 명의

**C. 투자·인증 정합성**
- C1. 투자유치 공시 이후 특허·인증 활동 정체
- C2. 벤처/이노비즈/메인비즈 인증 만료·임박한데 현재형으로 홍보

**D. 데이터 부재 처리**
- D1. 재무 미확보 → DART 조회 후 "초기단계, 공시의무 없음" 중립 표기 (안 봄과 구분)
- D2. 전 축 공백 → "근거 불충분, 판단 보류" 명시

> 트리거 임계값(예: "출원 정체 = 몇 개월")은 실제 데이터를 보며 W2에 숫자로 확정한다.

## 4. 데이터 소스 & API (전부 확정)

### 기술력 — KIPRISPlus (키: `KIPRIS_KEY`, 요청 호스트: plus.kipris.or.kr)
2단계 조회:
1. **공통 REST — 출원인 검색** (`CommonSearchService/CommonSearchApplicantInfo`)
   - 입력: `searchName`(기업명), `searchAddress`(주소로 동명이인 보정)
   - 출력: `PersonNumber`(특허고객번호) ← 회사 특정용
2. **특허·실용 항목별검색 — 전체검색** (`patUtiModInfoSearchSevice/getAdvancedSearch`)
   - 입력: `applicant`(출원인명 또는 PersonNumber), `lastvalue`(권리상태: 등록R/공개A/소멸F/무효I/거절J), `applicationDate`(YYYYMMDD~YYYYMMDD 범위), `patent`/`utility`, `numOfRows`(최대500), `sortSpec`+`descSort`
   - 출력: `totalCount`, `registerStatus`, `ipcNumber`, `applicationDate`, `applicantName`
   - ⚠️ 폐기예정인 `getWordSearch`(단어검색)는 쓰지 않는다. `getAdvancedSearch`를 쓴다.
   - ⚠️ 파라미터 키 이름이 오퍼레이션마다 `accessKey`/`ServiceKey`로 다를 수 있음 → 테스트 호출로 확인.
   - ⚠️ 개발단계 계정은 트래픽 한도 있음 → 데모 기업 결과는 캐싱.

### 투자이력 + 공신력 — 공공데이터포털 (키: `DATA_GO_KR_KEY`)
- **벤처기업명단** (data.go.kr) — 업체명·대표자·벤처확인유형(벤처투자/연구개발/혁신성장/예비벤처)·지역·업종·투자유치정보·인증유효기간
- 확인유형·유효기간이 공신력 축, 투자유치정보가 투자이력 축.

### 재무(보조) — OpenDART (키: `DART_KEY`)
- **고유번호(corp_code)** — 기업명→DART 식별자 매핑 (최초 1회 다운로드해 data/corp_code.xml 저장)
- **단일회사 주요 재무지표** — 매출·자본·자산 등 핵심 계정
- 초기 기업 대부분 DART에 없음 → 없으면 D1 중립 표기. 시리즈A+ 기업 대비용.

### (참고) K-Startup — 창업지원사업 "공고" 데이터. 평가축 아님. 향후 "활동성" 확장용으로만 보유.

## 5. MVP 스코프 (3주, 9/7 배포)

**포함**
1. 기업명(+주소) 입력 → 3소스 수집 (벤처확인·특허·DART보조)
2. 3축 근거형 점수 + 출처 링크 (심사의견서 첨부 가능한 형태)
3. 반증 엔진 (6규칙 red flag) → 각 플래그를 "심층심사 질의 문항"으로 변환 출력
4. 근거형 팩트시트 리포트 UI (심사 보조 톤: 플래그·체크리스트 중심, 투자매력 어필 아님)

**부가 데모 (주력 아님)**
- 적합 투자자·후속 지원사업 힌트 — 룰 기반, 단계·업종·테마 태그 정합 최소 수준

**제외**
- LangGraph 등 에이전트 (결정론적 파이프라인이라 불필요)
- 채용/기술스택 (원티드·잡코리아 API 승인 불확실 — 우선순위 낮음)
- 뉴스 (유료·종량제 전환, 안정성 위해 드롭)

## 6. 개발 일정

| 주차 | 목표 |
|---|---|
| W1 | 키 세팅, 각 소스 수집 모듈 프로토타입 (venture → dart → patent 순) |
| W2 | 정규화·개체연결(사업자/법인번호), 3축 집계, 반증 규칙 구현, 백엔드 API |
| W3 | React 근거형 리포트 UI(심사 보조 톤), 매칭 데모(부가), 배포, 기획서·기능명세서 PDF 마무리 |

## 7. 프로젝트 구조

```
venture-eval/
├── .env                    # KIPRIS_KEY, DATA_GO_KR_KEY, DART_KEY (git 제외)
├── collectors/
│   ├── venture.py          # 벤처기업명단
│   ├── patent.py           # KIPRIS 2단계 (공통→항목별검색)
│   └── dart.py             # 고유번호 + 단일회사 주요재무지표
├── core/
│   ├── normalize.py        # 개체 연결 (사업자/법인번호, 주소 보정)
│   ├── scoring.py          # 3축 결정론적 집계 (LLM 아님)
│   ├── falsify.py          # 반증 규칙 6개 (+ 플래그→질의문항 변환)
│   └── match.py            # (부가) 투자자·지원사업 힌트 — 룰 기반
├── data/
│   └── corp_code.xml       # DART 고유번호 (최초 1회)
└── main.py
```

## 8. 실무 주의 (배포)

- 배포 URL은 9/7~9/11 무조건 살아있어야 함 (결격 방지). 데모 기업 결과 캐싱 필수.
- API 키는 절대 커밋하지 않는다. `.env` + `.gitignore`.
- 개발단계 KIPRIS 계정 트래픽 한도 → 실시간 무제한 조회 가정 금지, 캐시 우선.
